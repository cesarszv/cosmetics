[project]
name = "cosmetics"
version = "0.1.0"
description = "Generador estático para inventario personal de cosméticos"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["apps/site/tests"]
pythonpath = ["apps/site"]
